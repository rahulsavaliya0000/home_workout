
import 'package:flutter/material.dart';

class Dashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Help & Support Screen'),
      ),
      body: Center(
        child: Text('Help & Support Screen Content'),
      ),
    );
  }
}